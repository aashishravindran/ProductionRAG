[pytest]
markers =
    retrieval: Retrieval-only tests — validate context recall without Ollama
    e2e: End-to-end tests — validate full answer quality (requires Ollama on localhost:11434)
testpaths = tests
